name: Keep Render App Awake

on:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Curl ping to Render health endpoint
        run: |
          curl --fail --silent --show-error https://<your-app-name>.onrender.com/health || exit 1
